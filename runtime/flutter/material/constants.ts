import { Icon } from "./../widgets/icon";
import { Icons } from "./icons";

export const kDeleteIconSize = 18.0;
export const kDefaultDeleteIcon = new Icon(Icons.cancel, {
    size: kDeleteIconSize,
});
